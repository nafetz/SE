ELEVATOR STATE:

- Zustandsübergänge in FixedClosed eingefügt

- Zustandsübergang in FixedOpen gefixt (war versehendelich auf FixedOpen)

- Richtungswechsellogik in FixedOpen hinzu

- Overload fertig



ELEVATOR:

- void SwitchDirection hinzu (ändert Fahrtrichtung)



PROGRAM

- while(true) schleife hinzu, kurbelt den Fahrstuhl an


DEFAULTS

- ManualResetEvent _wre  entspr. Property hinzu,
	damit der sich nicht dumm und dämlich rödelt, wenn niemad fahren will. 
	dazu bitte Defaults.WaitingResetEvent.Set(); bei jedem drückwunsch einfügen!!!!
	[das warten ist jedoch noch nicht implementiert.